package {{.name}}{{if .time}}

import "time"{{end}}

type {{.Name}} struct {
    {{ range $element := .fields }}
    {{$element.Name}} {{$element.type}} `json:"{{$element.name}}" gorm:"column:{{$element.column}}{{if $element.key}};primary_key{{end}}" bson:"{{$element.bson}}" dynamodbav:"{{$element.name}}" firestore:"{{$element.name}}" validate:"required{{if ne $element.length 0}},max={{$element.length}}{{end}}"`
    {{end}}
}
